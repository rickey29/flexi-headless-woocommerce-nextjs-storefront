# FlexiWoo Environment Variables
# Copy to .env.local for local development
# =========================================

# --- Logging ---
# LOG_LEVEL=debug              # debug | info | warn | error (default: debug dev, warn prod)
# LOG_JSON=false               # Structured JSON output (default: false dev, true prod)

# --- Error Tracking (optional) ---
# SENTRY_DSN=                  # Server-side Sentry DSN (secret, production only)
# NEXT_PUBLIC_SENTRY_DSN=      # Client-side Sentry DSN (public, production only)
