/**
 * Core Types Exports
 *
 * Pure TypeScript types used by both Core and Adapter layers.
 *
 * @module core/types
 */

// Logging types (used by core/config and adapter/logging)
export type { LogLevel, LogContext } from './logging';
